# 🐛 DEBUGGING & DIAGNOSTICS GUIDE

Полное руководство по отладке системы управления полётом.

---

## 📡 Как Проверить, Что Приёмник Работает?

### Шаг 1: Откройте Serial Monitor

```
Arduino IDE → Tools → Serial Monitor
Baudrate: 115200
```

### Шаг 2: Должны Видеть Вывод Каждые 500ms

```
===== FLIGHT CONTROLLER STATE =====
Time: 1234567 ms
Failsafe: NO
Armed: NO (waiting 2s)

RC CHANNELS:
CH1 (Aileron)   1500 µs
CH2 (Elevator)  1500 µs
CH3 (Throttle)  1000 µs
CH4 (Rudder)    1500 µs
CH5 (Flaps)     1500 µs
CH6 (Boost)     1000 µs
CH7-CH10: 1500 µs (default)

SERVO OUTPUTS:
Left Aileron   1500 µs
Right Aileron  1500 µs
Elevator       1500 µs
Motor (ESC)    1000 µs (OFF)

ARM Status: DISARMED
Boost: INACTIVE
===================================
```

### Шаг 3: Если Вывода Нет

**Проблема 1: Нет питания на ESP32**
```
✓ Проверить USB кабель
✓ Проверить, что светодиод питания горит
✓ Попробовать другой USB порт
```

**Проблема 2: Неправильная скорость Serial Monitor**
```
✗ НЕПРАВИЛЬНО: 9600 или 115200
✓ ПРАВИЛЬНО: 115200 baud (проверить в коде)
```

**Проблема 3: Неправильный COM порт**
```
Tools → Board → ESP32-C3
Tools → Port → Выбрать COM порт (должен быть активным)
```

---

## 🎚️ Как Проверить, Что Приёмник Слышит Пульт?

### Проверка 1: Сигнал Есть?

Если в Serial Monitor видны каналы, которые меняются:
```
✓ ХОРОШО: Приёмник слышит пульт

Когда двигаете stick:
CH1 (Aileron) меняется: 1000 → 1500 → 2000
CH2 (Elevator) меняется: 1000 → 1500 → 2000
CH3 (Throttle) меняется: 1000 → 1500 → 2000
```

### Проверка 2: Failsafe НЕ Срабатывает?

```
Failsafe: NO  ✓ Хорошо
Failsafe: YES ✗ Потеря сигнала!
```

Если `Failsafe: YES` при включённом пульте:
```
✗ Приёмник не слышит пульт
✗ Кабель приёмника не подключен
✗ Батарея пульта разряжена
✗ Разная страна/частота (2.4 GHz регионы)
```

### Проверка 3: Значения Каналов Адекватные?

```
ХОРОШО:         ПЛОХО:
1000–2000 µs    0–5000 µs
1500 µs центр   500 µs центр
Гладкие зменения Скачкообразные/шум
```

---

## 🔒 Как Вооружить Систему (ARM)?

### Шаг 1: Убедитесь, что Failsafe НЕ Срабатывает
```
Failsafe: NO  ← Главное требование!
```

### Шаг 2: Проверьте Газ На МИНИМУМЕ
```
CH3 (Throttle): < 1050 µs  ✓
```

### Шаг 3: Удерживайте Газ 2 Секунды
```
Throttle LOW для 2 сек
  ↓
Serial Monitor:
  "Time: 0 ms"  ← начало отсчёта
  "Time: 1000 ms"
  "Time: 2000 ms"
  "Armed: YES" ← Система вооружена!
```

### Если ARM Не Происходит?

```
Проблема: Failsafe включен?
✓ Проверить: Failsafe в Serial Monitor должен быть NO

Проблема: Газ не в минимуме?
✓ Проверить: CH3 должен быть < 1050 µs (около 1000)

Проблема: Прошло < 2 сек?
✓ Подождать 2 полные секунды

Проблема: Газ был поднят посередине?
✗ Если газ поднялся хоть немного → таймер сбросится
✓ Держать газ НИЗКО всё 2 сек без перерыва
```

---

## 🛩️ Как Проверить Сервоприводы?

### Проверка 1: Сервоприводы Получают PWM?

```
Если сервоприводы НЕ двигаются (статичны):

✗ Проблема: PWM сигнал не идёт на GPIO
  ✓ Решение: Проверить соединения GPIO4, GPIO5, GPIO6

✗ Проблема: Сервопривод разряжен
  ✓ Решение: Проверить напряжение батареи

✗ Проблема: Сервопривод сломан
  ✓ Решение: Попробовать другой сервопривод
```

### Проверка 2: Направление Сервоприводов

При движении stick ВЛЕВО (CH1 = 1000):
```
✓ Left Aileron:  идёт вверх (>1500 µs)
✓ Right Aileron: идёт вниз (<1500 µs)

✗ Наоборот → поменять соединения GPIO4 и GPIO5

✗ Оба в одну сторону → проблема в mixer коде
```

При движении stick ВВЕРХ (CH2 = 1000):
```
✓ Elevator: идёт вверх (>1500 µs)

✗ Вниз → поменять соединение GPIO6
```

### Проверка 3: Диапазон Движения

```
Stick LEFT (1000 µs):
  Left Aileron:  ~1000 µs   (максимум вверх)
  Right Aileron: ~2000 µs   (максимум вниз)

Stick CENTER (1500 µs):
  Left Aileron:  ~1500 µs   (нейтраль)
  Right Aileron: ~1500 µs   (нейтраль)

Stick RIGHT (2000 µs):
  Left Aileron:  ~2000 µs   (максимум вниз)
  Right Aileron: ~1000 µs   (максимум вверх)
```

Если диапазон неправильный:
```
✗ Значения меньше (например, 1250–1750):
  ✓ Проверить коэффициент в ControlMixer

✗ Значения больше (например, 900–2100):
  ✓ Проверить Config::PWM_MIN и PWM_MAX
```

---

## 🚀 Как Проверить Мотор?

### Проверка 1: ESC Получает Сигнал?

Поднимите газ:
```
CH3 (Throttle): 1500 µs (50%)
Motor (ESC):    1500 µs   ← должно совпадать!
```

Если совпадает → ESC получает сигнал ✓

Если НЕ совпадает → проверить GPIO7 соединение

### Проверка 2: Мотор Включается?

```
Убедитесь:
✓ Система ARMED (Armed: YES)
✓ Failsafe НЕ срабатывает (Failsafe: NO)
✓ Газ поднят выше минимума (CH3 > 1050 µs)

Если соблюдены все условия:
  • Звук от ESC должен измениться (тон выше при газе)
  • Пропеллер должен начать вращаться (медленнее → быстрее)
```

### Проблема: Мотор Не Вращается

```
✗ ESC не подал питание
  ✓ Проверить батарею (должна быть полная)
  ✓ Проверить соединения питания на ESC

✗ Сигнал не достигает ESC
  ✓ Проверить GPIO7 соединение
  ✓ Проверить, что PWM выходит на GPIO7

✗ ESC в режиме калибровки
  ✓ Выключить, переподключить питание

✗ Мотор заблокирован
  ✓ Убедиться, что пропеллер свободно вращается
```

---

## ⚠️ Как Проверить Failsafe?

### Тест 1: Отключите Приёмник (Потеря Сигнала)

```
1. Система вооружена, газ поднят
2. Выключите пульт
3. Через ~500ms должно случиться:
   ✓ Failsafe: YES
   ✓ Armed: NO (автоматически разоружена)
   ✓ Motor: 1000 µs (выключен!)
   ✓ Servo: 1500 µs (нейтраль)
```

Если этого НЕ произойдёт:
```
✗ Failsafe логика не работает
  ✓ Проверить IBusReceiver.isSignalLost()
  ✓ Проверить Config::RX_TIMEOUT_US (должно быть 500000)
```

### Тест 2: Включите Приёмник Обратно

```
1. Включите пульт
2. Удержите газ на минимуме 2 сек
3. Должно произойти:
   ✓ Failsafe: NO (сигнал восстановлен)
   ✓ Armed: YES (система вновь вооружена)
   ✓ Motor: может работать
```

---

## 📊 Как Проверить Boost Режим?

### Тест: Активируйте Boost

```
1. Система ARMED
2. Поднимите газ: CH3 = 1300 µs
   Motor должно быть: 1300 µs

3. Включите Boost (CH6 = 1800 µs)
   Motor должно стать: 1550 µs (1300 + 250)
   Это +250 µs boost!

4. Через 5 сек Boost отключится
   Motor вернётся: 1300 µs

5. Чтобы повторить Boost:
   ✓ Вернуть CH6 в LOW (< 1250 µs)
   ✓ Затем повторить шаг 3
```

Если Boost НЕ работает:
```
✗ CH6 значение не меняется
  ✓ Проверить, что переключатель подключен

✗ Motor не увеличивается на 250 µs
  ✓ Проверить ThrottleManager boost логику
  ✓ Проверить Config::THROTTLE_BOOST (должно быть 250)
```

---

## 🔧 Быстрая Диагностика (Чек-лист)

```
□ Система подана питание (LED горит)
□ Serial Monitor показывает вывод (115200 baud)
□ Приёмник видит пульт (каналы меняются)
□ Failsafe НЕ срабатывает (Failsafe: NO)
□ Газ может быть выключен (CH3 < 1050 µs)
□ Система ARM-ится (после 2 сек газа внизу)
□ Сервоприводы двигаются при движении stick
□ Мотор вращается при поднятии газа
□ Failsafe срабатывает при потере сигнала
□ Boost активируется на 5 сек
```

Если что-то НЕ работает → посмотреть Serial Monitor вывод!

---

## 🆘 Типичные Проблемы и Решения

### "Приёмник Не Видит Пульт"

```
Признаки:
  Failsafe: YES при включённом пульте
  Каналы застыли на 1500 µs
  Мотор не вращается

Решение:
  1. Проверить, что пульт включен
  2. Проверить, что батарея пульта полная
  3. Проверить соединение приёмника на GPIO3
  4. Проверить, что приёмник и пульт на одной частоте
  5. Перезагрузить приёмник (питание вкл/выкл)
```

### "Мотор Не Вращается, Хотя Газ Поднят"

```
Признаки:
  CH3 меняется, но Motor PWM = 1000 µs
  Система не ARM-ится

Решение:
  1. Проверить, что система ARMED (Armed: YES)
  2. Проверить, что failsafe НЕ включен (Failsafe: NO)
  3. Проверить GPIO7 соединение ESC
  4. Проверить батарею (может быть разряжена)
  5. Проверить, что пропеллер свободно вращается
```

### "Сервоприводы Дёргаются Или Прыгают"

```
Признаки:
  Servo PWM скачет: 1400–1600 µs
  Servo движется рывками

Решение:
  1. Проверить помехи (подальше от USB кабеля)
  2. Проверить батарею (низкое напряжение = помехи)
  3. Проверить соединения (плохой контакт)
  4. Проверить, что приёмник не рядом с мотором
```

### "Failsafe Срабатывает Случайно"

```
Признаки:
  Failsafe: YES → NO → YES
  Каналы исчезают и появляются

Решение:
  1. Проверить соединение приёмника (может быть ненадежное)
  2. Проверить питание приёмника (может быть нестабильное)
  3. Проверить помехи (микроволновка, WiFi)
  4. Удалить приёмник подальше от электроники
```

---

## 💡 Советы По Отладке

1. **Используйте Serial Monitor постоянно**
   - Открывайте его ПЕРЕД любым тестом
   - Смотрите на логи при проблемах

2. **Меняйте по одному**
   - Если поменялось несколько вещей → меняйте по одной
   - Так сразу видно, что сломалось

3. **Проверьте Соединения**
   - 80% проблем — это плохие соединения
   - Переподключите все провода

4. **Проверьте Питание**
   - Низкое напряжение → много странных ошибок
   - Батарея должна быть полная

5. **Перезагружайтесь**
   - Выключите питание, подождите 10 сек, включите
   - Иногда это помогает при странных ошибках

---

**Created for OpenPlaneProject 🛩️**
